# Backend Server Configuration
# Update this with your actual backend server URL

# For local development (same device)
# API_BASE_URL=http://localhost:5000/api/auth

# For Android emulator connecting to local server
# API_BASE_URL=http://10.0.2.2:5000/api/auth

# For physical device on same network (replace with your computer's IP)
# API_BASE_URL=http://192.168.1.100:5000/api/auth

# For production
# API_BASE_URL=https://your-domain.com/api/auth

# Google OAuth Configuration
# Get this from Google Cloud Console (Web Client ID)
GOOGLE_WEB_CLIENT_ID=YOUR_WEB_CLIENT_ID.apps.googleusercontent.com
